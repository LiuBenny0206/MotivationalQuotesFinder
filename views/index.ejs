<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/styles/main.css">
  <title>I need a motivation</title>
</head>

<body>
  <div class="container">
    <h1>Let's find a beautiful quote</h1>
    <% if (locals.error) { %>
      <div id="tag-error" class="tag-error">
        <%= error %>
      </div>
    <% } %>

    <% if (locals.quote) { %>
      <section id="cards" class="cards">
        <article class="card-item">
          <h2 class="card-activity">
            <%= quote.body %>
          </h2>
          <div class="card-info">
            <span class="card-type">
              Author: <%= quote.author %>
            </span>
            <span class="card-participants">
              Favorites Count: <%= quote.favorites_count %>
            </span>
          </div>
        </article>
      </section>
    <% } %>
  </div>

</body>

</html>